name: CI

on:
  pull_request:
    branches: [develop, main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -e "apps/api[dev]"
      - name: Ruff check
        run: cd apps/api && ruff check .
      - name: Ruff format check
        run: cd apps/api && ruff format --check .
      - name: Bandit security scan
        run: cd apps/api && bandit -r app -ll --exclude .venv,tests -q || true

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -e "apps/api[dev]" aiosqlite
      - name: Run tests with coverage
        run: cd apps/api && python -m pytest --cov=app --cov-report=term-missing --cov-fail-under=75 -q
